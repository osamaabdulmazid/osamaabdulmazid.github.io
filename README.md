# Osama Abdul Mazid — Personal Brand Portfolio

This version keeps the existing dark-purple futuristic visual design and reframes the CV as a personal-brand portfolio.

## Edit later
Most visible content is in `index.html`, so you can change text directly without relying on `data.js`.

## GitHub Pages
Keep `index.html`, `styles.css`, and `profile.jpg` in the repository root.

If `profile.jpg` is already in the GitHub repository, keep it there.
